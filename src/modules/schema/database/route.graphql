
type Route {
  id: ID! @unique
  name: String
  path: String! @unique
  exact: Boolean! @default(value: false)
  component: String!
  Parent: Route @relation(name: "Routes")
  Childs: [Route!]! @relation(name: "Routes")
}